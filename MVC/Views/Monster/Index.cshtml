@model IEnumerable<Models.MonsterModels.MonsterListItem>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MonsterId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MonsterName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Creator)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Type)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ChallengeRating)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.HasLegendary)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.MonsterId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MonsterName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Creator)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Type)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ChallengeRating)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.HasLegendary)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.MonsterId }) |
            @Html.ActionLink("Details", "Details", new { id = item.MonsterId }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.MonsterId })
        </td>
    </tr>
}

</table>
